#pragma once
#include <stdio.h>

void question151(){
	printf("Bboth require syscalls, which run in kernel mode. "
		"This is because peripheral devices like the hardware clock "
		"and external storage are managed by the OS, and are not "
		"available to user mode programs. ");
}

void question152(){
	printf("");
}

void question153(){
	printf("");
}